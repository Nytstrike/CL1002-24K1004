// Create a program that takes an array of size 6 and shifts all its elements to the right
// by one position. The last element should move to the first position.
// Input: {1,2,3,4,5,6}
// Output: {6,1,2,3,4,5}
#include <stdio.h>
int main (){
    int in[6],temp[6],tem,i,j;
    for (i=0;i<=5;i++){
        printf("Enter Numbers:");
        scanf("%d",&in[i]);
    }
    temp[0]=in[6];
    for (j=0;j<=5;j++){
        temp[j+1]=in[j+1];
        printf("%d, ",temp[j]);
    }
}
